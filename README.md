# IOC-scraper


Scrapes websites to find IOCs for malwares mentioned in `input_file.txt` and stores them in `output_file.csv`

Usage:
```python
scrapy crawl google -a inp=input_file.txt -o output_file.csv
```
